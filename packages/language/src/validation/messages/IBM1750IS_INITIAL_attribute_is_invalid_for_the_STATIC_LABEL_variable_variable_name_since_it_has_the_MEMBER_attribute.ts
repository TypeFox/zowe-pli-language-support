

import { ValidationAcceptor } from "langium";
import { Bound } from "../../generated/ast";

export function IBM1750IS_INITIAL_attribute_is_invalid_for_the_STATIC_LABEL_variable_variable_name_since_it_has_the_MEMBER_attribute(bound: Bound, accept: ValidationAcceptor): void {
  //TODO IBM1750IS INITIAL attribute is invalid for the STATIC LABEL variable variable- name since it has the MEMBER attribute.
  //The INITIAL attribute is supported for a STATIC LABEL variable only if the variable is a scalar or an array of scalars.
}